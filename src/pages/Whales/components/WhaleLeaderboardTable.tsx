import type { WhaleWindow } from "domain/synthetics/whales/period";

// Stub — replaced by Task 8.
export function WhaleLeaderboardTable({ window: _window }: { window: WhaleWindow }) {
  return null;
}
