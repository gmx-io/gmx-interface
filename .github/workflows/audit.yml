name: Audit

on:
  pull_request:
    branches:
      - master
      - release
      - "release-*"
    types: [opened, edited, synchronize, reopened]
  workflow_dispatch:

jobs:
  audit:
    name: Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run security audit
        run: yarn npm audit --severity moderate --no-deprecations --ignore 1112686

      - name: Install SDK dependencies
        run: |
          cd sdk
          yarn install --frozen-lockfile

      - name: Run SDK security audit
        run: |
          cd sdk
          yarn npm audit --severity moderate --no-deprecations --ignore 1112686
